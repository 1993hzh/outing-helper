<view class="view-container">
  <van-tabbar active="{{ active }}" bind:change="onChange">
    <van-tabbar-item wx:if="{{!item.hide}}" wx:for="{{list}}" wx:key="index" icon="{{item.icon}}" info="{{item.info}}">
      {{item.text}}
    </van-tabbar-item>
  </van-tabbar>
</view>